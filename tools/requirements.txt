# Dependencias do site (MkDocs Material) -- usadas pelo CI e pela montagem local.
#
# Este arquivo existe por dois motivos:
#   1. Fixar a versao, para o site publicado nao mudar de aparencia sozinho
#      quando o mkdocs-material lanca uma versao nova.
#   2. Dar ao actions/setup-python um manifesto para o cache de pip apontar.
#      Sem manifesto, "cache: pip" falha o job inteiro com
#      "No file matched to [**/requirements.txt or **/pyproject.toml]".
#
# Para instalar localmente:  pip install -r tools/requirements.txt
mkdocs-material==9.7.6
